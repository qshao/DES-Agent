# Bundled Offline Artifacts

These small JSON artifacts let the offline DES viscosity and metal-binding workflows run without network access.

- `designsolvents/viscosity/model.json`
- `stability_constants/model.json`

They are intentionally lightweight reference artifacts, not the full upstream model checkpoints.
