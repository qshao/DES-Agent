# Offline Artifacts

This directory contains local model artifacts used by the offline predictor adapters.

- `designsolvents/viscosity/model.json` is used by the DES viscosity adapter.
- `stability_constants/model.json` is used by the metal-binding stability-constant adapter.

The manifest at [`manifest.yaml`](./manifest.yaml) maps workflow names to local artifact paths.
