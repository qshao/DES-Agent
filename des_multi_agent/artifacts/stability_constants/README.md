# Stability Constant Artifacts

Place the offline metal-ligand stability model files here. The bundled example uses a small local JSON artifact so the repository can run offline without external downloads.
